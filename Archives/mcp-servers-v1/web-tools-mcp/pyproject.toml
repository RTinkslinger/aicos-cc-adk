[project]
name = "web-tools-mcp"
version = "0.1.0"
description = "Web Tools MCP Server — browse, scrape, search"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0.0",
    "python-dotenv>=1.0",
    "playwright>=1.49",
    "httpx>=0.27",
]
