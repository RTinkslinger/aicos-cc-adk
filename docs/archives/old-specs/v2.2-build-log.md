# v2.2 Build Log — Testing Reference

**Created:** 2026-03-16
**Purpose:** Track what was built in each chunk and what needs testing (upstream/downstream connections).
**Branch:** `feat/three-agent-architecture`

---

## Chunk 1: Infrastructure

### Files Created
- `sql/v2.2-migrations.sql` — 3 new tables (sync_metadata, cai_inbox, notifications) + notion_synced columns
- `.claude/agents/web-researcher.md` — Full-toolkit web subagent definition
- `.claude/agents/content-worker.md` — Lightweight batch content worker subagent
- `infra/state-mcp.service` — systemd unit for State MCP (port 8000)
- `infra/web-tools-mcp.service` — systemd unit for Web Tools MCP (port 8001)
- `infra/content-agent.service` — systemd unit for Content Agent (no port)
- `infra/sync-agent.service` — systemd unit for Sync Agent (no port)
- `infra/health_check.sh` — Health check script for 2 MCP servers

### Files Modified
- `deploy.sh` — Updated for v2.2 service names, skills sync, subagent sync

### Upstream Dependencies
- Existing Postgres instance on droplet
- Existing Cloudflare tunnels (mcp.3niac.com, web.3niac.com)

### Downstream Consumers
- **State MCP** uses cai_inbox, notifications, sync_metadata tables
- **Content Agent** writes to thesis_threads, actions, content_digests with notion_synced=false
- **Sync Agent** reads notion_synced=false rows, updates sync_metadata
- **All agents** use the notion_synced column convention

### Testing Checklist
- [ ] Run `v2.2-migrations.sql` on droplet: `ssh root@aicos-droplet "psql $DATABASE_URL < /dev/stdin" < sql/v2.2-migrations.sql`
- [ ] Verify 3 new tables: `psql $DATABASE_URL -c '\dt' | grep -E 'sync_metadata|cai_inbox|notifications'`
- [ ] Verify notion_synced on thesis_threads: `psql $DATABASE_URL -c "SELECT column_name FROM information_schema.columns WHERE table_name='thesis_threads' AND column_name='notion_synced'"`
- [ ] Verify subagent .md files are discovered by Agent SDK with `setting_sources=["project"]`
- [ ] Deploy with updated deploy.sh and verify all 4 services start
- [ ] Health check script works: `bash infra/health_check.sh`

---

## Chunk 2: State MCP

### Files Created
- `state/__init__.py`
- `state/server.py` — FastMCP server, 5 tools, port 8000
- `state/db/__init__.py`
- `state/db/connection.py` — asyncpg connection pool
- `state/db/thesis.py` — thesis CRUD for State MCP tools
- `state/db/inbox.py` — cai_inbox read/write
- `state/db/notifications.py` — notifications read/write
- `state/tests/test_state_mcp.py`

### Upstream Dependencies
- Postgres tables: thesis_threads, cai_inbox, notifications (from Chunk 1 migrations)
- `asyncpg` package (added to pyproject.toml)
- `DATABASE_URL` environment variable

### Downstream Consumers
- **CAI (Claude.ai)** calls all 5 tools via mcp.3niac.com
- **Content Agent** reads cai_inbox via psql (NOT via State MCP)
- **Sync Agent** reads notifications via psql (NOT via State MCP)

### Testing Checklist
- [ ] Unit tests pass: `python3 -m pytest state/tests/ -v`
- [ ] Server starts: `python3 -m state.server` → `curl http://localhost:8000/health`
- [ ] `get_state()` returns thesis threads and notifications
- [ ] `create_thesis_thread("Test", "Test thesis")` writes to Postgres with notion_synced=false
- [ ] `update_thesis("Test", "Evidence text", "for")` updates Postgres
- [ ] `post_message("track_source", "Test content")` writes to cai_inbox
- [ ] `health_check()` returns OK with DB connectivity
- [ ] Cloudflare tunnel (mcp.3niac.com → localhost:8000) works after repointing from old sync-agent

---

## Chunk 3: Web Tools MCP

### Files Created
- `web/task_store.py` — In-memory async task state management

### Files Modified
- `web/server.py` — Added web_task_submit/status/result, exposed check_strategy/manage_session/validate
- `web/tools.py` — Adjusted for v2.2 (removed old web_task, added external wrappers)
- `web/agent.py` — Wired up web skills via setting_sources

### Upstream Dependencies
- Existing web/lib/ modules (browser, scrape, search, etc.)
- Playwright browser on droplet
- ANTHROPIC_API_KEY for web_task brain
- FIRECRAWL_API_KEY for search/scrape

### Downstream Consumers
- **CAI** uses web_task_submit/status/result (async pattern via web.3niac.com)
- **Content Agent** uses direct tools (web_browse, web_scrape, web_search, etc.) via http://localhost:8001/mcp
- **Content Agent subagents** (web-researcher) use same direct tools

### Testing Checklist
- [ ] Server starts: `python3 -m web.server` → `curl http://localhost:8001/health`
- [ ] `web_scrape("https://example.com")` returns content
- [ ] `web_browse("https://example.com")` returns page snapshot
- [ ] `web_search("AI agents")` returns results
- [ ] `check_strategy` exposed as external tool (not just SDK @tool)
- [ ] `manage_session` exposed as external tool
- [ ] `validate` exposed as external tool
- [ ] `web_task_submit({"task": "scrape example.com"})` returns task_id
- [ ] `web_task_status(task_id)` returns status
- [ ] `web_task_result(task_id)` returns result when complete
- [ ] Cloudflare tunnel (web.3niac.com → localhost:8001) works

---

## Chunk 4: Skills (14 files)

### Files Created
- `skills/web/strategy.md` — Tool selection, cost ladder, readiness ladder, UCB bandit
- `skills/web/anti-detection.md` — Persona profiles, stealth, fingerprint coherence
- `skills/web/auth-escalation.md` — 5-step auth ladder
- `skills/web/content-validation.md` — Quality scoring, retry strategy
- `skills/web/youtube-extraction.md` — Playlist handling, dedup, cookies
- `skills/content/analysis.md` — IDS methodology, content analysis framework
- `skills/content/scoring.md` — 5-factor scoring formula + weights + thresholds
- `skills/content/thesis-reasoning.md` — Conviction spectrum, key questions lifecycle
- `skills/content/publishing.md` — Digest JSON format, git push, Vercel deploy
- `skills/content/inbox-handling.md` — CAI Inbox message types, routing, watch list
- `skills/sync/conflict-resolution.md` — Human-owned fields win, AI-vs-AI timestamp wins
- `skills/sync/notion-patterns.md` — Notion API patterns, property formatting
- `skills/sync/change-interpretation.md` — Change event interpretation, action generation
- `skills/data/postgres-schema.md` — All 8 tables, column types, query patterns

### Upstream Dependencies
- Knowledge extracted from existing v1 code (web/lib/*, content/lib/*, sync/lib/*)
- CONTEXT.md domain knowledge
- docs/notion/ Notion operations guide

### Downstream Consumers
- **Content Agent** loads content skills + data skills + web skills
- **Sync Agent** loads sync skills + data skills
- **web_task brain** loads web skills
- **web-researcher subagent** loads web skills
- **content-worker subagent** loads content skills + data skills

### Testing Checklist
- [ ] All 14 .md files exist in skills/ directory tree
- [ ] Skills contain actionable instructions (not just descriptions)
- [ ] postgres-schema.md includes all 8 tables with column types
- [ ] scoring.md includes the 5-factor formula and thresholds
- [ ] Agents can load skills via Skill tool with setting_sources=["project"]
- [ ] Deploy puts skills at /opt/agents/skills/ on droplet

---

## Chunk 5: Content Agent

### Files Created
- `content/runner.py` — asyncio timer + query() launcher (1-min inbox + 5-min content)
- `content/system_prompt.md` — v2.2 brain (replaces v1 system prompt)

### Upstream Dependencies
- Agent SDK (`query()`, `ClaudeAgentOptions`, `AgentDefinition`)
- Web Tools MCP at http://localhost:8001/mcp (for direct web tools)
- Skills at /opt/agents/skills/ (loaded via Skill tool)
- Subagent defs at /opt/agents/.claude/agents/ (web-researcher.md, content-worker.md)
- cai_inbox table (read via psql)
- ANTHROPIC_API_KEY, DATABASE_URL, FIRECRAWL_API_KEY env vars

### Downstream Consumers
- **Postgres** — writes thesis_threads, actions, content_digests (notion_synced=false)
- **notifications table** — writes notifications for CAI
- **aicos-digests repo** — publishes digests via git push
- **Sync Agent** — picks up unsynced rows

### Testing Checklist
- [ ] Runner starts without errors: `python3 -m content.runner`
- [ ] Inbox check session runs: query() completes, checks cai_inbox
- [ ] Content cycle session runs: query() completes, processes watch list
- [ ] Agent can use Bash + psql to read/write Postgres
- [ ] Agent can call Web Tools MCP (mcp__web__web_scrape etc.)
- [ ] Agent can load skills via Skill tool
- [ ] Agent can spawn web-researcher subagent via Agent tool (run_in_background)
- [ ] Agent can spawn content-worker subagents via Agent tool
- [ ] E2E: post_message → cai_inbox → Content Agent processes → Postgres write

---

## Chunk 6: Sync Agent

### Files Created
- `sync/runner.py` — asyncio timer + query() launcher (10-min sync cycle)
- `sync/system_prompt.md` — v2.2 brain (replaces v1 system prompt)

### Upstream Dependencies
- Agent SDK (`query()`, `ClaudeAgentOptions`)
- Skills at /opt/agents/skills/ (sync skills + data skills)
- Postgres tables (all 8, especially notion_synced column)
- NOTION_TOKEN for Notion API access
- ANTHROPIC_API_KEY, DATABASE_URL env vars

### Downstream Consumers
- **Notion** — pushes unsynced rows to Notion databases
- **Postgres** — updates notion_synced=true after push, updates sync_metadata
- **notifications table** — writes notifications for significant sync events
- **change_events table** — logs detected changes

### Testing Checklist
- [ ] Runner starts without errors: `python3 -m sync.runner`
- [ ] Sync cycle session runs: query() completes, finds unsynced rows
- [ ] Agent can use Bash + psql to read Postgres
- [ ] Agent can load sync skills via Skill tool
- [ ] Flag-based sync: finds rows with notion_synced=false
- [ ] Notion push: writes to Notion, updates notion_synced=true
- [ ] Human-owned field pull: reads Status/Outcome from Notion, updates Postgres
- [ ] Conflict resolution: human-owned fields (Status, Outcome) always win
- [ ] sync_metadata table updated after each cycle

---

## Chunk 7: Integration + Cutover

### Files Modified
- `deploy.sh` — Updated for v2.2 (4 services, skills sync, subagent sync)

### Testing Checklist
- [ ] Deploy script runs end-to-end: `./deploy.sh`
- [ ] All 4 services running: `systemctl status state-mcp web-tools-mcp content-agent sync-agent`
- [ ] State MCP health: `curl http://localhost:8000/health`
- [ ] Web Tools MCP health: `curl http://localhost:8001/health`
- [ ] E2E CAI inbox flow: post_message → Content Agent processes → Postgres write
- [ ] E2E web_task: web_task_submit → web_task_status → web_task_result
- [ ] E2E content pipeline: content cycle → YouTube extraction → analysis → Postgres → digest
- [ ] E2E sync: unsynced rows → Notion push → sync_metadata updated
- [ ] Memory usage acceptable: `free -m` (target: <2GB for all 4 processes)
- [ ] Old services stopped: sync-agent(old), web-agent(old), content-agent(old)
- [ ] Cloudflare tunnels: mcp.3niac.com → 8000 (State MCP), web.3niac.com → 8001 (Web Tools MCP)

---

## Cross-Chunk Dependencies

```
Chunk 1 (Infra) ──────────────► Chunk 2 (State MCP) ──► Chunk 7 (Integration)
     │                                                        ▲
     ├──────────────────────────► Chunk 3 (Web Tools MCP) ────┤
     │                                                        │
     └──────────────────────────► Chunk 4 (Skills) ───────────┤
                                      │                       │
                                      ├──► Chunk 5 (Content)──┤
                                      └──► Chunk 6 (Sync) ────┘
```

All chunks depend on Chunk 1 (Postgres tables exist).
Chunks 5+6 depend on Chunk 4 (skills exist for agents to load).
Chunk 7 depends on everything else.
